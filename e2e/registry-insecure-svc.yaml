apiVersion: v1
kind: Service
metadata:
  name: feast-registry-insecure
  namespace: aniket-cursor-test
spec:
  selector:
    feast.dev/name: byos-spark-e2e
  ports:
  - name: grpc
    port: 80
    targetPort: 6571
    protocol: TCP
